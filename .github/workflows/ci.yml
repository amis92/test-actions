name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - shell: pwsh
      run: |
        dotnet tool install wham -g
    - name: test pwsh
      shell: pwsh
      run: |
        Write-Output "output"
        Write-Host "host"
        Write-Information "info"
        Write-Warning "warning"
        Write-Verbose "verbose"
    - name: Print event
      env:
        JSON: ${{ toJson(github.event) }}
      run: echo "$JSON"
